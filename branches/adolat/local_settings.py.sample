DEBUG = True
#DEBUG = False

STATIC_URL = '/static_adolat/'

DATABASES = {'default': {'ENGINE': 'django.db.backends.sqlite3', 'NAME': '/var/liba/adolat/adolat.db'}}
CACHES = {
    'default': {
        'BACKEND': 'django.core.cache.backends.memcached.MemcachedCache',
        'LOCATION': 'unix:/tmp/memcached.sock',
    }
}